// two float number summation in c program...
#include <stdio.h>

int main() {
    float x, y, result;
    x = 45.5;
    y = 56.6;
    result = x + y;
    printf("total floating is %f", result);
return 0;
}
