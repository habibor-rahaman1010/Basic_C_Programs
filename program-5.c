// three number sum in c program..
# include <stdio.h>

int main() {
    int x, y, z, result;
    x = 67;
    y = 43;
    z = 54;
    printf("Total summation are %d \n", ( result = x + y + z));
    printf("Result is %d", result);

return 0;
}
