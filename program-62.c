//two ingeter number summation input as from user in c program...

#include <stdio.h>

int main() {
    int a, b, sum;
    printf("Enter your value of a: ");
    scanf("%d", &a);

    printf("Enter your value of b: ");
    scanf("%d", &b);
    sum = (a + b);

    printf("Summation is: %d\n", sum);

return 0;
}

