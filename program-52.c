//two number summation  in c program...
#include <stdio.h>

int main () {
    int a;
    int b;
    int sum;

    a = 50;
    b = 60;
    sum = (a + b);
    printf("Summation is %d + %d = %d", a, b, sum);

return 0;
}
