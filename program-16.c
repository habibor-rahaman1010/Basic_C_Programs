//user input output program in c....
#include <stdio.h>

int main() {
    int a;
    printf("Enter your valu of a: \n");
    scanf("%d", &a);
    printf("value of a is: %d", a);

    float x, y;
    printf("Enter your value of: x, y \n");
    scanf("%f %f", &x, &y);
    printf("values of x: %f and y %f \n", x, y);

return 0;
}
