//print a namta usin c progam...
#include <stdio.h>

int main() {\
    int i, base;
    base = 3;
    for(i = 1; i <= 10; i++){
        printf("%d x %d = %d \n", base, i, (base*i));
    }

return 0;
}
