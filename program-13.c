#include <stdio.h>

int main() {
    double x, y, z, result;
    x = 10;
    y = 13;
    z = 17;
    result = (x * y * z);
    printf("Result is: %lf \n", result);
    printf("Result is: %.2lf \n", result);

return 0;
}
