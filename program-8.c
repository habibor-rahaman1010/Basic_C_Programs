// three number divited in c program.....
#include <stdio.h>

int main() {
    int x, y, z, result;
    x = 34;
    y = 2;
    z = 3;
    result = (x / y / z);
    printf("Result is: %d", result);

return 0;
}
