// three number substraction in c program.....
#include <stdio.h>

int main() {
    int x, y, z, result;
    x = 34;
    y = 23;
    z = 45;
    result = (x - y - z);
    printf("Result is: %d", result);

return 0;
}
